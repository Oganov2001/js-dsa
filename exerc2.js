const groceriesList = ['milk', 'bread', 'eggs', 'flour', 'choose', 'sugar'];

const searchForItem = (item) = {
    
};

// Using ES6 methods.
/*
const searchForItem = (item) => {
    return groceriesList.filter(groceryItem => groceryItem === item);
};
*/

console.log(searchForItem("eggs"));